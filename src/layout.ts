/**
 * Layout-Konstanten, die sowohl JavaScript als auch CSS kennen müssen.
 * Der CSS-Gegenwert steht als `--sheet-peek` in src/styles/tokens.css.
 */

/** Höhe des eingefahrenen Explore-Panels in px. */
export const SHEET_PEEK_PX = 204
